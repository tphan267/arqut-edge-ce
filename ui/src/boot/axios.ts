import { boot } from 'quasar/wrappers';

// Axios boot file (currently not used since we have api client in utils)
// This is required by quasar.config.ts boot configuration

export default boot(() => {
  // Nothing to do here - we use our custom API client
});
